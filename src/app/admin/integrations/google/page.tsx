import { GoogleSettings } from "@/integrations/google/ui/google-settings";

export const metadata = { title: "Google sign-in settings" };

export default function Page() {
  return <GoogleSettings />;
}
